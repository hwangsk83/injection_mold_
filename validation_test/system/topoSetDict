/*--------------------------------*- C++ -*----------------------------------*\
  Version:     12
  Format:      ascii
  Class:       dictionary
  Object:      topoSetDict
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

actions
(
    {
        name    inletCells;
        type    cellSet;
        action  new;
        source  boxToCell;
        box     (-0.025 0.024 -0.004) (0.025 0.026000000000000002 0.006);
    }

    {
        name    inletFaces;
        type    faceSet;
        action  new;
        source  cellToFace;
        set     inletCells;
        option  all;
    }
);
